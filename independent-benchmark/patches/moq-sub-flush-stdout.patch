diff --git a/moq-sub/src/media.rs b/moq-sub/src/media.rs
--- a/moq-sub/src/media.rs
+++ b/moq-sub/src/media.rs
@@ -63,6 +63,8 @@ impl<O: AsyncWrite + Send + Unpin + 'static> Media<O> {
             };
             let buf = self.download_first_object(init_track_name, "init").await?;
-            self.output.lock().await.write_all(&buf).await?;
+            let mut output = self.output.lock().await;
+            output.write_all(&buf).await?;
+            output.flush().await?;
             let mut reader = Cursor::new(&buf);
 
             let ftyp = read_atom(&mut reader).await?;
@@ -194,7 +196,9 @@ impl<O: AsyncWrite + Send + Unpin + 'static> Media<O> {
             let out = out.clone();
             let buf = Self::recv_object(object).await?;
 
-            out.lock().await.write_all(&buf).await?;
+            let mut output = out.lock().await;
+            output.write_all(&buf).await?;
+            output.flush().await?;
         }
 
         Ok(())
